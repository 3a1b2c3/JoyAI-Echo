/** 短视频首帧参考图上传；false 时 UI 仍显示但不可选图/拖入。 */
export const FIRST_FRAME_UPLOAD_ENABLED = true;

/** Local/offline deployment has no useful queue estimate; don't show busy dialogs. */
export const GENERATION_BUSY_ALERT_ENABLED = false;
