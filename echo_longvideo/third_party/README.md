# Third-party runtime sources

`scripts/setup_msst.py` checks out the pinned MSST-WebUI source here. The
checkout is intentionally ignored by the parent repository so MSST-WebUI
remains a separate AGPL-3.0 dependency with its own Git history and license.

Expected runtime path:

```text
third_party/MSST-WebUI/
```
