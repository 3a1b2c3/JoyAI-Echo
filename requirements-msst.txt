# Minimal inference-only dependencies for the pinned MSST-WebUI Bandit model.
# Training/UI dependencies are intentionally excluded.
librosa>=0.10.2,<0.12
soundfile>=0.12,<1
ml-collections>=0.1.1,<2
omegaconf>=2.3,<3
