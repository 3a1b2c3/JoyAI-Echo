# =============================================================================
# Open-source DMD inference - Python dependencies
#
# These pins match the conda environment we developed and tested against
# (CUDA 13.0). For PyTorch family wheels with CUDA, install from the
# official PyTorch index, e.g.:
#
#     pip install --index-url https://download.pytorch.org/whl/cu130 \
#         torch==2.9.1 torchvision==0.24.1 torchaudio==2.9.1
#
# Then install the rest of the requirements:
#
#     pip install -r requirements.txt
#
# If you prefer conda, use `environment.yml` instead.
# =============================================================================

# --- core deep-learning stack ---
torch==2.9.1
torchvision==0.24.1
torchaudio==2.9.1
triton==3.5.1

# --- HF transformers (Gemma 3 text encoder) ---
transformers==4.57.6
safetensors==0.6.2

# --- numerical / utilities ---
numpy>=2.2,<3
scipy>=1.13
einops>=0.8
Pillow>=10
tqdm>=4.66

# --- video / audio I/O ---
av>=14.0
